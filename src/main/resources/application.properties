spring.application.name=tienda-electronica

# ========= CONEXIÓN LOCAL (XAMPP) =========
spring.datasource.url=jdbc:mysql://localhost:3306/tienda_electronica?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Render también usa esta línea para leer PORT
server.port=${PORT:8080}
